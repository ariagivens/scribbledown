@page {
    margin-bottom: 5pt;
    margin-top: 5pt;
}

p {
    display: block;
    font-family: "{{font_family}}", serif;
    font-style: normal;
    font-weight: normal;
    font-size: 1rem;
    line-height: 1.2;
    text-align: left;
    text-indent: 1em;
    margin: 0;
    padding: 0;
}
